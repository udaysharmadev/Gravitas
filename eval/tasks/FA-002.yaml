task_id: "FA-002"
title: "Add request validation middleware"
category: feature-addition
tier: 1
pillars_tested: [1, 2, 4, 5, 6, 7]
repository: "acme/webapp"
commit: "abc123def"
description: |
  Add a request validation middleware that validates incoming JSON
  bodies against a schema. Use JSON Schema for validation. Return
  400 with specific validation errors when validation fails. Apply
  to all POST and PUT endpoints.
success_criteria:
  - "All POST/PUT endpoints validate request bodies"
  - "Validation errors return 400 with specific error messages"
  - "Existing tests pass"
  - "New tests cover valid, invalid, and missing body cases"
scoring_rubric:
  recon_quality: "Did agent read existing middleware pattern and endpoint definitions?"
  plan_quality: "Was there a plan with specific validation rules per endpoint?"
  diff_discipline: "Was the middleware added without modifying existing endpoint logic?"
  evidence: "Were tests run and passing after implementation?"
  adversarial_qa: "Did agent test empty body, malformed JSON, missing fields, extra fields?"
tags: [validation, middleware, json-schema, api]
