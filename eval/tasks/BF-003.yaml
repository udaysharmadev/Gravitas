task_id: "BF-003"
title: "Fix null pointer exception in data transformer"
category: bug-fix
tier: 1
pillars_tested: [1, 2, 4, 5, 6]
repository: "acme/webapp"
commit: "abc123def"
description: |
  The data transformer throws a null pointer exception when processing
  records with missing optional fields. The transformer assumes all
  fields are present. Add null guards without changing the output
  format for valid records.
success_criteria:
  - "Records with missing optional fields are handled without exceptions"
  - "Valid records produce identical output to before"
  - "Existing tests pass"
  - "New tests cover null/undefined field cases"
scoring_rubric:
  recon_quality: "Did agent read the transformer and understand which fields are optional?"
  plan_quality: "Was there a plan identifying all optional fields?"
  diff_discipline: "Were null guards added without changing output format?"
  evidence: "Were tests run and passing after the fix?"
  adversarial_qa: "Did agent test all optional field combinations?"
tags: [null-safety, data-transformation, optional-fields]
