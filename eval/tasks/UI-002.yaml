task_id: "UI-002"
title: "Add dark mode toggle to settings page"
category: ui-change
tier: 2
pillars_tested: [1, 2, 3, 4, 5, 6, 7, 9]
repository: "acme/webapp"
commit: "abc123def"
description: |
  Add a dark mode toggle to the settings page. When toggled,
  the entire application should switch between light and dark
  themes. Persist the user's preference in localStorage. Use
  CSS custom properties for theming.
success_criteria:
  - "Toggle appears on settings page"
  - "Toggling switches all UI elements to dark/light theme"
  - "Preference persists across page reloads"
  - "No flash of unstyled content on page load"
  - "Existing tests pass"
  - "Visual verification confirms both themes"
scoring_rubric:
  recon_quality: "Did agent read the settings page, existing theme system, and CSS structure?"
  plan_quality: "Was there a plan with theme implementation strategy?"
  diff_discipline: "Was the toggle added without modifying existing component logic?"
  evidence: "Were tests run and visual verification performed for both themes?"
  adversarial_qa: "Did agent test toggle, persistence, flash prevention, and all pages?"
  reporting: "Did response lead with outcome and visual evidence?"
tags: [ui, dark-mode, theming, toggle, localStorage]
