task_id: "SM-001"
title: "Add created_at and updated_at timestamps to users table"
category: schema-migration
tier: 3
pillars_tested: [1, 2, 3, 5, 6]
repository: "acme/webapp"
commit: "abc123def"
description: |
  Add created_at (TIMESTAMPTZ, NOT NULL, DEFAULT NOW()) and
  updated_at (TIMESTAMPTZ, NOT NULL, DEFAULT NOW()) columns to
  the users table. Update the application layer to set updated_at
  on every write. Ensure existing rows get reasonable timestamps.
success_criteria:
  - "Migration adds both columns with correct types and defaults"
  - "Existing rows get created_at = updated_at = migration time"
  - "Application sets updated_at on every write"
  - "All existing tests pass"
  - "New tests verify timestamp behavior"
scoring_rubric:
  recon_quality: "Did agent read the schema, migration history, and application write paths?"
  plan_quality: "Was there a migration plan with rollback strategy?"
  diff_discipline: "Was the migration scoped to the users table?"
  evidence: "Were tests run and passing after migration?"
  adversarial_qa: "Did agent test existing rows, new rows, and update behavior?"
tags: [database, migration, timestamps, schema]
