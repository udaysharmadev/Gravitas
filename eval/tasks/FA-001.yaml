task_id: "FA-001"
title: "Add rate limiting to API endpoints"
category: feature-addition
tier: 2
pillars_tested: [1, 2, 3, 4, 5, 6, 7]
repository: "acme/webapp"
commit: "abc123def"
description: |
  Add rate limiting to all public API endpoints. Use a token bucket
  algorithm with configurable limits per endpoint. Default: 100
  requests per minute. Return 429 with Retry-Header when exceeded.
success_criteria:
  - "All public API endpoints enforce rate limits"
  - "Token bucket algorithm implemented correctly"
  - "429 response includes Retry-After header"
  - "Rate limits are configurable via environment variables"
  - "Existing tests pass"
  - "New tests cover rate limiting behavior"
scoring_rubric:
  recon_quality: "Did agent read the routing layer, middleware pattern, and existing tests?"
  plan_quality: "Was there a numbered plan with risk analysis and tier classification?"
  diff_discipline: "Was the implementation scoped to the middleware and route config?"
  evidence: "Were tests run and passing after implementation?"
  adversarial_qa: "Did agent test burst traffic, rate limit reset, and header format?"
  reporting: "Did response lead with outcome and cite evidence?"
tags: [rate-limiting, middleware, api, security]
