task_id: "RF-003"
title: "Extract repeated API client logic into base class"
category: refactor
tier: 2
pillars_tested: [1, 2, 4, 5, 6, 7]
repository: "acme/webapp"
commit: "abc123def"
description: |
  Three API client classes (UserClient, OrderClient, ProductClient)
  share identical retry logic, error handling, and request formatting.
  Extract the shared logic into a base ApiClient class. Each
  subclass should only define its endpoints and data types.
success_criteria:
  - "Base ApiClient class with shared retry, error handling, formatting"
  - "Each subclass only defines endpoints and types"
  - "All existing tests pass"
  - "No behavior change in any client"
scoring_rubric:
  recon_quality: "Did agent read all 3 clients and identify the exact shared logic?"
  plan_quality: "Was there a plan with class hierarchy and method extraction?"
  diff_discipline: "Were changes scoped to the 3 clients + new base class?"
  evidence: "Were tests run and passing with same count?"
  adversarial_qa: "Did agent verify retry behavior, error handling, and request format?"
  reporting: "Did response lead with structural change and behavior preservation?"
tags: [refactor, inheritance, api-client, deduplication]
