task_id: "RF-002"
title: "Convert callback-based code to async/await"
category: refactor
tier: 1
pillars_tested: [1, 2, 4, 5, 6, 7]
repository: "acme/webapp"
commit: "abc123def"
description: |
  The file src/services/data.ts uses nested callbacks for async
  operations. Convert to async/await while preserving exact
  behavior. Do not change any business logic, error handling,
  or return values.
success_criteria:
  - "All callbacks converted to async/await"
  - "Error handling preserved exactly"
  - "Return values preserved exactly"
  - "All existing tests pass"
  - "No behavior change"
scoring_rubric:
  recon_quality: "Did agent read the file and understand the callback patterns?"
  plan_quality: "Was there a plan with specific conversion for each callback?"
  diff_discipline: "Were changes scoped to async patterns without modifying logic?"
  evidence: "Were tests run and passing with same count?"
  adversarial_qa: "Did agent verify error propagation and return values?"
  reporting: "Did response lead with pattern change and behavior preservation?"
tags: [refactor, async-await, callbacks, patterns]
